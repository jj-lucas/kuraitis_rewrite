# Migration `20201229121926-add-autoincremental-order-number`

This migration has been generated by Lucas Gabriel Salina D'Ovidio at 12/29/2020, 1:19:26 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Order" (
    "number" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "id" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "shipping" TEXT NOT NULL,
    "shippingCosts" TEXT NOT NULL,
    "total" INTEGER NOT NULL,
    "currency" TEXT NOT NULL,
    "charge" TEXT NOT NULL,
    "shippedAt" DATETIME,
    "fullfilled" BOOLEAN NOT NULL DEFAULT false,
    "archived" BOOLEAN NOT NULL DEFAULT false,
    "trackingCode" TEXT,
    "customerId" TEXT NOT NULL,
    "locale" TEXT NOT NULL,

    FOREIGN KEY ("customerId") REFERENCES "Customer"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Order" ("id", "createdAt", "shipping", "shippingCosts", "total", "currency", "charge", "shippedAt", "fullfilled", "archived", "trackingCode", "customerId", "locale") SELECT "id", "createdAt", "shipping", "shippingCosts", "total", "currency", "charge", "shippedAt", "fullfilled", "archived", "trackingCode", "customerId", "locale" FROM "Order";
DROP TABLE "Order";
ALTER TABLE "new_Order" RENAME TO "Order";
CREATE UNIQUE INDEX "Order.id_unique" ON "Order"("id");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201229105940-add-locale-to-order..20201229121926-add-autoincremental-order-number
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -133,9 +133,10 @@
     orderId       String?
 }
 model Order {
-    id            String         @id @default(cuid())
+    number        Int            @id @default(autoincrement())
+    id            String         @unique @default(cuid())
     createdAt     DateTime       @default(now())
     items         PurchasedSku[]
     shipping      String
     shippingCosts String
```


