# Migration `20210102133155-remove-unnecessary-field`

This migration has been generated by Lucas Gabriel Salina D'Ovidio at 1/2/2021, 2:31:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Order" (
    "number" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "id" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "shippedAt" DATETIME,
    "shipping" TEXT NOT NULL,
    "shippingCosts" TEXT NOT NULL,
    "total" INTEGER NOT NULL,
    "currency" TEXT NOT NULL,
    "charge" TEXT NOT NULL,
    "archived" BOOLEAN NOT NULL DEFAULT false,
    "trackingCode" TEXT,
    "customerId" TEXT NOT NULL,
    "locale" TEXT NOT NULL,

    FOREIGN KEY ("customerId") REFERENCES "Customer"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Order" ("id", "createdAt", "shipping", "shippingCosts", "total", "currency", "charge", "shippedAt", "archived", "trackingCode", "customerId", "locale", "number") SELECT "id", "createdAt", "shipping", "shippingCosts", "total", "currency", "charge", "shippedAt", "archived", "trackingCode", "customerId", "locale", "number" FROM "Order";
DROP TABLE "Order";
ALTER TABLE "new_Order" RENAME TO "Order";
CREATE UNIQUE INDEX "Order.id_unique" ON "Order"("id");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201229121926-add-autoincremental-order-number..20210102133155-remove-unnecessary-field
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -136,17 +136,16 @@
 model Order {
     number        Int            @id @default(autoincrement())
     id            String         @unique @default(cuid())
     createdAt     DateTime       @default(now())
+    shippedAt     DateTime?
     items         PurchasedSku[]
     shipping      String
     shippingCosts String
     total         Int
     currency      String
     charge        String
     customer      Customer       @relation(fields: [customerId], references: [id])
-    shippedAt     DateTime?
-    fullfilled    Boolean        @default(false)
     archived      Boolean        @default(false)
     trackingCode  String?
     customerId    String
     locale        String
```


